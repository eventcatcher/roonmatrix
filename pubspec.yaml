name: roonmatrix
description: This app is the frontend for the roonmatrix LED-Matrix Hardware to display device and playout data, and control the playout.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.2.3+55

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0" # build with flutter 3.29.2, Dart 3.7.2 on macos
 
# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  bitsdojo_window: ^0.1.6
  file_selector: ^1.0.3
  easy_debounce: ^2.0.3
  collection: ^1.19.1
  flutter_bloc: ^9.1.0
  equatable: ^2.0.7
  built_value: ^8.9.5
  built_value_generator: ^8.9.5
  built_collection: ^5.1.1
  bloc_concurrency: ^0.3.0
  uuid: ^4.5.1
  macos_ui: 2.2.0
  path_provider: ^2.1.5
  text_scroll: ^0.2.0
  styled_text: ^8.1.0
  flutter_svg: ^2.0.17
  permission_handler: ^11.4.0
  shared_preferences: ^2.5.3
  dio: ^5.8.0+1
  validators: ^3.0.0
  url_launcher: ^6.3.1
  menu_bar: ^0.5.3
  package_info_plus: ^8.3.0
  flutter_window_close: ^1.2.0
  language_code: ^0.5.5
  window_manager: ^0.4.3
  font_awesome_flutter: ^10.8.0
  animated_gradient: ^0.0.3
  screen_retriever: ^0.2.0
  top_snackbar_flutter: ^3.2.0
  hovering: ^1.0.4
  web_socket_channel: ^3.0.3
  crypto: ^3.0.6
  #updatable_ticker: ^1.0.6
  flutter_inappwebview: ^6.1.5
  extended_text: ^15.0.2
  connection_network_type: ^1.0.0
  connectivity_plus: ^6.1.5
  device_info_plus: ^12.3.0
  flutter_layout_grid: ^2.0.8
  json_repair_flutter: ^2.0.2
  updatable_ticker: ^1.1.2
  updatable_vertical_ticker: ^1.1.1

dependency_overrides:

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  build_runner: ^2.4.15
  flutter_launcher_icons: ^0.14.3
  msix: ^3.16.8

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  remove_alpha_ios: true
  image_path_ios: "assets/icon/roundedIcon.png"
  image_path: "assets/icon/icon.png"
  web:
    generate: false
    image_path: "assets/icon/icon.png"
    background_color: "#ffffff"
    theme_color: "#ffffff"
  windows:
    generate: true
    image_path: "assets/icon/icon.png"
    icon_size: 256 # min:48, max:256, default: 48
  macos:
    generate: true
    image_path: "assets/icon/roundedIcon.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages

  assets:
  # svg
    - assets/svg/8-8-led-matrix-display-unit.svg
  # png
    - assets/icon/icon.png
    - assets/icon/roon.png
    - assets/icon/spotify.png
    - assets/icon/spotifyconnect.png
    - assets/icon/applemusic.png
  # data
    - assets/json/translations_en.json
    - assets/json/translations_de.json
    - assets/json/translations_fr.json

  fonts:
    - family: Lightdot
      fonts:
        - asset: assets/fonts/Lightdot-8x8.ttf
    - family: Arial
      fonts:
        - asset: assets/fonts/ARIAL.TTF
 
msix_config:
  display_name: RoonMatrix
  publisher_display_name: Stephan Wilhelm
  publisher: "CN=FBB63578-BCA1-45A6-8B91-36157AAE0E70"
  identity_name: StephanWilhelm.Roonmatrix
  logo_path: assets/icon/icon.png
  trim_logo: false
  capabilities: privateNetworkClientServer
  languages: en-us, de-de
  output_path: C:\build\msix
  output_name: roonmatrix
  store: true
  install_certificate: false
  signtool_options: "/v"

